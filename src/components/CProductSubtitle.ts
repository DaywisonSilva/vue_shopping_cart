import styled from "vue-styled-components";

const CSubtitle = styled.h1`
    font-family: 'Poppins Light', Helvetica, sans-serif;
    font-size: 1rem;
    font-weight: normal;
    color: #ddd;
    text-transform: capitalize;
`

export default CSubtitle;